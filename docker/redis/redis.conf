# Redis 설정 파일

# 네트워크 설정
bind 0.0.0.0
port 6379

# 보안 설정
# requirepass your-redis-password

# 메모리 설정
maxmemory 256mb
maxmemory-policy allkeys-lru

# 지속성 설정
save 900 1
save 300 10
save 60 10000

# 로그 설정
loglevel notice
logfile ""

# 기타 설정
timeout 0
tcp-keepalive 300